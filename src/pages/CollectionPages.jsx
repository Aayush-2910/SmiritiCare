import React, { useState } from 'react';
import { Link, useParams } from 'react-router-dom';
import { useStore, unlockedAchievements } from '../state.jsx';
import { Button, Icon, Photo, Avatar, PageHeader, EmptyState, Progress, Toggle, AccessibilityPanel } from '../components/ui.jsx';
import { MemoryCard, MemoryForm, MemoryDetail, categories, FamilyForm, FamilyProfile, ChallengeForm } from '../components/MemorySystem.jsx';
import GardenScene, { GameArt } from '../components/GardenScene.jsx';
import { games } from '../data/games.js';
import { achievements } from '../data/achievements.js';
import { gardenStages } from '../data/garden.js';
import { images } from '../data/images.js';
export function MemoriesPage(){const{data,setModal}=useStore();const[category,setCategory]=useState('All memories');const[search,setSearch]=useState('');const[sort,setSort]=useState('recent');const filtered=data.memories.filter(m=>(category==='All memories'||category==='Favorites'?category!=='Favorites'||m.favorite:m.category===category)&&`${m.title} ${m.people} ${m.location}`.toLowerCase().includes(search.toLowerCase())).sort((a,b)=>sort==='oldest'?a.date.localeCompare(b.date):b.date.localeCompare(a.date));return <><PageHeader eyebrow="THE STORIES THAT MAKE YOU, YOU" title="My little book of memories" description="Familiar faces. Favorite places. Moments that never get old." action={<Button icon="Plus" onClick={()=>setModal({title:'Give a moment a home',content:<MemoryForm/>})}>Add a memory</Button>}/><div className="album-banner"><Icon name="Heart" size={25}/><p><strong>{data.memories.length} beautiful moments,</strong> and so many more to come.</p><span>Made of love, kept with care.</span></div><div className="filter-bar"><div className="search-field"><Icon name="Search" size={18}/><input aria-label="Search memories" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Find a familiar moment…"/></div><select aria-label="Sort memories" value={sort} onChange={e=>setSort(e.target.value)}><option value="recent">Newest moments first</option><option value="oldest">Oldest moments first</option></select></div><div className="filter-pills" aria-label="Memory categories">{categories.map(c=><button key={c} aria-pressed={c===category} className={category===c?'active':''} onClick={()=>setCategory(c)}>{c==='Favorites'&&<Icon name="Heart" size={15}/>} {c}</button>)}</div>{filtered.length?<div className="memory-grid">{filtered.map(m=><MemoryCard key={m.id} memory={m}/>)}</div>:<EmptyState icon="Images" title="Your album has room for another story." description="Try a different search, or add a lovely new moment."><Button onClick={()=>setModal({title:'A new little memory',content:<MemoryForm/>})}>Add a memory</Button></EmptyState>}</>;}
export function MemoryRoute(){const{id}=useParams();return <><PageHeader title="A moment to remember" action={<Button variant="secondary" to="/app/memories" icon="ArrowLeft">Back to album</Button>}/><section className="card memory-route"><MemoryDetail id={id}/></section></>;}
export function FamilyPage(){const{data,setModal}=useStore();return <><PageHeader eyebrow="YOUR PEOPLE, ALWAYS CLOSE" title="Love makes a lovely little garden." description="A familiar voice, a shared memory, a reason to smile." action={<Button icon="UserPlus" onClick={()=>setModal({title:'Welcome someone special',content:<FamilyForm/>})}>Add family</Button>}/><div className="family-connection-banner"><span>❤️</span><div><h3>Distance changes nothing about together.</h3><p>Send a little memory. Make someone’s whole day.</p></div><Button variant="secondary" icon="ImagePlus" onClick={()=>setModal({title:'Share a little memory',content:<MemoryForm/>})}>Send a memory</Button></div><div className="family-grid">{data.family.map(p=><article className="family-card card" key={p.id}><button className="family-profile-open" onClick={()=>setModal({title:'A familiar face',content:<FamilyProfile id={p.id}/>})}><Avatar src={p.photo} name={p.name} size="large"/><h2>{p.name}</h2><span className="pill sage">{p.relation}</span></button><p className="family-birthday"><Icon name="Cake" size={17}/>{p.birthday}</p><blockquote>“{p.memory}”</blockquote><Button to={`/app/messages?person=${p.id}`} variant="secondary" icon="MessageCircle">Say a little hello</Button><button className="text-link" onClick={()=>setModal({title:'A happy little question',content:<ChallengeForm/>})}>Create a challenge <Icon name="ArrowUpRight" size={16}/></button></article>)}</div>{!data.family.length&&<EmptyState title="Invite someone special to your garden."/>}<section className="card family-challenges"><div className="section-heading horizontal compact-heading"><div><h2>Made with love. Remembered together.</h2><p>Gentle challenges from the people who know you best.</p></div><Button icon="Plus" onClick={()=>setModal({title:'Create a family challenge',content:<ChallengeForm/>})}>New challenge</Button></div>{data.challenges.length?data.challenges.map(c=><Link className="challenge-row" key={c.id} to="/app/games/family-memory"><span className="feature-icon peach"><Icon name="Heart"/></span><div><strong>{c.question}</strong><p>A question from Rahul · Ready to play</p></div><Icon name="ArrowRight"/></Link>):<p className="muted">Every family has a story only they know. Turn one of yours into a little challenge.</p>}</section></>;}
export function GameHub(){const{data}=useStore();const[category,setCategory]=useState('All activities');const categories=['All activities','Memory','Focus','Recognition','Sequence','Everyday Life','Family'];return <><PageHeader eyebrow="NO RUSH. NO PRESSURE. JUST PLAY." title="Let’s give your mind a little joy." description="Choose something that feels good today. Every little moment counts."/><div className="games-welcome"><div><span className="pill sage"><Icon name="Sparkles" size={14}/> TODAY’S LITTLE PICK</span><h2>Familiar faces.<br/>A warm feeling.</h2><p>A little family recognition game to brighten your day.</p><Button to="/app/games/who-is-this">Let’s see who’s here <Icon name="ArrowRight" size={18}/></Button></div><GameArt type="faces"/></div><div className="filter-pills" aria-label="Game categories">{categories.map(c=><button className={category===c?'active':''} aria-pressed={category===c} key={c} onClick={()=>setCategory(c)}>{c}</button>)}</div><div className="game-grid">{games.filter(g=>category==='All activities'||g.category===category).map(g=>{const count=data.completed.filter(c=>c.game===g.id).length;return <article className="game-card" key={g.id}><Link to={`/app/games/${g.id}`} className={`game-art-wrap ${g.color}`} aria-label={`Play ${g.title}`}><GameArt type={g.art}/><span className="game-duration"><Icon name="Clock" size={14}/>{g.time}</span></Link><div className="game-card-body"><div className="game-card-meta"><span className="eyebrow">{g.category}</span><span className="small muted">{g.difficulty}</span></div><h2>{g.title}</h2><p>{g.description}</p><div className="game-history"><Icon name={count?'CircleCheck':'Sprout'} size={15}/>{count?`${count} lovely ${count===1?'visit':'visits'}`:'A new little adventure'}</div><Button to={`/app/games/${g.id}`} variant="secondary">Let’s play <Icon name="ArrowRight" size={17}/></Button></div></article>;})}</div></>;}
export function GardenPage(){const{data,setModal}=useStore();const count=data.completed.length;const stage=[...gardenStages].reverse().find(s=>count>=s.at);const next=gardenStages.find(s=>s.at>count);return <><PageHeader eyebrow="YOUR EFFORT, BEAUTIFULLY GROWN" title="Welcome to your memory garden." description="Stay a while. There’s always something lovely growing here." action={<span className="pill sage"><Icon name="Sun" size={18}/> A peaceful spring day</span>}/><section className="full-garden card"><div className="garden-stats"><span><Icon name="Sprout"/><strong>{count}</strong> memory seeds</span><span><Icon name="Sparkles"/><strong>{data.points}</strong> journey points</span><span><Icon name="Images"/><strong>{data.memories.length}</strong> stories kept</span></div><GardenScene large/><div className="garden-stage-card"><span>{stage.emoji}</span><div><p className="eyebrow">YOUR GARDEN IS {stage.name.toUpperCase()}</p><h3>{count?'Every little moment made this.':'Lovely things start small.'}</h3><p>{next?`${next.at-count} more ${next.at-count===1?'activity':'activities'} until “${next.name}”.`:'A beautiful garden, full of your moments.'}</p><Progress label="Next garden stage" value={next?(count/next.at)*100:100}/></div><Button to="/app/games" icon="Plus">Plant another moment</Button></div></section><div className="garden-unlocks">{[{icon:'🌱',title:'Memory Seed',at:1,text:'A new beginning with every activity.'},{icon:'🌸',title:'Memory Flower',at:5,text:'Five lovely activities in your garden.'},{icon:'🦋',title:'Little Visitors',at:3,text:'Three moments invite a new butterfly.'},{icon:'🏡',title:'Memory Home',at:10,text:'Ten activities make this place a home.'}].map(item=><button className={`card unlock-card ${count>=item.at?'earned':''}`} key={item.title} onClick={()=>setModal({title:item.title,content:<div className="garden-discovery"><span>{item.icon}</span><p>{item.text}</p><p>{count>=item.at?'This little treasure is part of your garden.':`${Math.max(0,item.at-count)} more activities to make this yours.`}</p><Button to="/app/games" onClick={()=>setModal(null)}>Enjoy a little activity</Button></div>})}><span>{item.icon}</span><h3>{item.title}</h3><small>{count>=item.at?'Beautifully grown':'A little joy ahead'}</small><Icon name={count>=item.at?'Check':'LockKeyhole'} size={16}/></button>)}</div></>;}
export function JourneyPage(){const{data}=useStore();const[filter,setFilter]=useState('All moments');const week=data.activities.filter(a=>Date.now()-new Date(a.date).getTime()<7*86400000);const filtered=data.activities.filter(a=>filter==='All moments'||(filter==='Activities'?a.kind==='game':filter==='Memories'?a.kind==='memory':a.kind==='family')).sort((a,b)=>b.date.localeCompare(a.date));const groups=filtered.reduce((acc,a)=>{const day=new Date(a.date).toLocaleDateString('en-US',{weekday:'long',month:'long',day:'numeric'});(acc[day]??=[]).push(a);return acc;},{});return <><PageHeader eyebrow="ONE LOVELY MOMENT AT A TIME" title="Look how your story is growing." description="Not a race. Not a score. Just the little things that made your days."/><div className="journey-summary">{[['Puzzle','Activities',week.filter(a=>a.kind==='game').length],['Images','Memories',week.filter(a=>a.kind==='memory').length],['Heart','Family moments',week.filter(a=>a.kind==='family').length],['Award','Achievements',unlockedAchievements(data).length]].map(([icon,label,n])=><article className="card" key={label}><Icon name={icon} size={25}/><strong>{n}</strong><span>{label}</span><small>{label==='Achievements'?'Earned so far':'This week'}</small></article>)}</div><div className="filter-pills">{['All moments','Activities','Memories','Family'].map(f=><button className={filter===f?'active':''} key={f} onClick={()=>setFilter(f)} aria-pressed={filter===f}>{f}</button>)}</div><section className="timeline">{Object.entries(groups).map(([day,activities])=><div className="timeline-day" key={day}><span className="timeline-dot"/><h3>{day}</h3>{activities.map(a=><article className="timeline-item card" key={a.id}><span className={`feature-icon ${a.kind==='game'?'sage':a.kind==='memory'?'amber':'peach'}`}><Icon name={a.kind==='game'?'Puzzle':a.kind==='memory'?'Images':a.kind==='family'?'Heart':'Sprout'} size={23}/></span><div><h4>{a.title}</h4><p>{a.kind==='game'?'A little seed planted. +20 journey points.':a.kind==='family'?'A little closer to the people you love.':'Another moment in your beautiful story.'}</p></div><small>{new Date(a.date).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</small></article>)}</div>)}</section>{!filtered.length&&<EmptyState title="Your next lovely moment is waiting." description="Play a little game, save a story, or say hello to family."><Button to="/app/games">Find a little activity</Button></EmptyState>}</>;}
export function AchievementsPage(){const{data,setModal}=useStore();const unlocked=unlockedAchievements(data);return <><PageHeader eyebrow="LITTLE MILESTONES, LOTS OF HEART" title="So much worth celebrating." description="Every small step is something lovely. These are a few of yours."/><div className="achievement-banner"><span>🌷</span><div><h2>{unlocked.length?`${unlocked.length} little reasons to feel proud.`:'Your first little milestone is waiting.'}</h2><p>Celebrate the moments, not the numbers.</p></div><Button variant="secondary" to="/app/games">Find a little joy <Icon name="ArrowRight" size={17}/></Button></div><div className="achievements-grid">{achievements.map(a=><button className={`achievement-card card ${unlocked.includes(a.id)?'earned':''}`} key={a.id} onClick={()=>setModal({title:a.title,content:<div className="garden-discovery"><span>{a.emoji}</span><h3>{unlocked.includes(a.id)?'You made this little moment happen.':'A lovely little goal ahead.'}</h3><p>{a.description}</p><Button to={a.type==='memories'?'/app/memories':'/app/games'} onClick={()=>setModal(null)}>{a.type==='memories'?'Visit your album':'Enjoy an activity'}</Button></div>})}><span className="achievement-emblem">{a.emoji}</span><span className="eyebrow">{unlocked.includes(a.id)?'BEAUTIFULLY EARNED':'A LITTLE JOY AHEAD'}</span><h2>{a.title}</h2><p>{a.description}</p><span className={`pill ${unlocked.includes(a.id)?'sage':''}`}><Icon name={unlocked.includes(a.id)?'Check':'LockKeyhole'} size={15}/>{unlocked.includes(a.id)?'Part of your story':'Keep growing'}</span></button>)}</div></>;}
export function ProfilePage(){const{data,update,notify}=useStore();const[editing,setEditing]=useState(false);const[form,setForm]=useState(data.profile);return <><PageHeader eyebrow="A LITTLE ABOUT YOU" title="Your own lovely corner." description="A space that feels like you."/><section className="profile-card card"><div className="profile-cover"/><Avatar src={data.session==='family'?images.rahul:images.sunita} name={data.profile.name} size="large"/><h2>{data.session==='family'?'Rahul Sharma':`${data.profile.name} Sharma`}</h2><p className="muted">{data.session==='family'?'Family companion':'Memory maker. Garden grower. Loved very much.'}</p><div className="profile-stats">{[[data.completed.length,'Activities'],[data.memories.length,'Memories'],[unlockedAchievements(data).length,'Achievements'],[data.family.length,'Connections']].map(([n,l])=><div key={l}><strong>{n}</strong><span>{l}</span></div>)}</div><Button variant="secondary" icon="Pencil" onClick={()=>setEditing(!editing)}>{editing?'Close editor':'Edit senior profile'}</Button>{editing&&<form className="form profile-form" onSubmit={e=>{e.preventDefault();update(d=>({...d,profile:form}));setEditing(false);notify('Your little corner has been updated.');}}><label>First name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Demo age<input type="number" min="18" max="120" value={form.age} onChange={e=>setForm({...form,age:Number(e.target.value)})}/></label><label>Favorite things<textarea value={form.favorites} onChange={e=>setForm({...form,favorites:e.target.value})}/></label><Button type="submit">Save profile</Button></form>}<div className="profile-about"><p className="eyebrow">THINGS THAT MAKE YOU SMILE</p><p>{data.profile.favorites}</p><span className="pill sage">{data.profile.age} years of stories</span></div></section></>;}
export function SettingsPage(){const{data,update,notify}=useStore();const s=data.settings;return <><PageHeader eyebrow="FEEL RIGHT AT HOME" title="A little more your way." description="Simple preferences for a comfortable little space."/><div className="settings-grid"><section className="card settings-card"><h2><Icon name="Palette"/>Appearance</h2><p className="muted">Choose the feeling of your space.</p><div className="appearance-options">{[['light','Light'],['soft','Soft'],['contrast','High contrast']].map(([value,label])=><button key={value} className={s.appearance===value?'active':''} onClick={()=>update(d=>({...d,settings:{...d.settings,appearance:value}}))} aria-pressed={s.appearance===value}><span className={`appearance-swatch ${value}`}/>{label}{s.appearance===value&&<Icon name="Check" size={16}/>}</button>)}</div><h2 className="settings-subheading"><Icon name="Accessibility"/>Your comfort</h2><AccessibilityPanel/></section><div><section className="card settings-card"><h2><Icon name="Bell"/>Little nudges</h2><p className="muted">Choose your preferences. No real notifications are sent.</p>{[['memories','Family memories'],['journey','Daily journey'],['reminders','Reminders'],['achievements','Achievements']].map(([key,label])=><Toggle key={key} label={label} checked={s.notifications[key]} onChange={checked=>update(d=>({...d,settings:{...d.settings,notifications:{...d.settings.notifications,[key]:checked}}}))}/>)}</section><section className="card settings-card privacy-card"><h2><Icon name="ShieldCheck"/>Your stories, with care</h2><p>Our vision: your personal memories are shared only with people you choose.</p><p className="note small"><strong>In this demo:</strong> there is no secure account or cloud storage. Everything stays in this browser, where anyone with device access can view it. Please use sample content.</p><Button to="/app/profile" variant="secondary" icon="UserRound">Profile settings</Button><button className="text-link" onClick={()=>{update(d=>({...d,settings:{...d.settings,textSize:1,contrast:false,motion:false,sound:false,appearance:'light'}}));notify('Your appearance preferences are back to their defaults.');}}>Reset appearance preferences</button></section></div></div></>;}
